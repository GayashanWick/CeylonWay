import packageSchema from './schemas/package';
import postSchema from './schemas/post';
import gearSchema from './schemas/gear';

export const schema = {
  types: [packageSchema, postSchema, gearSchema],
};
